# ── Env vars — sourced for EVERY shell invocation ───────────────────────────
export EDITOR='nvim'
export VISUAL='nvim'

export GOPATH="${XDG_DATA_HOME:-$HOME/.local/share}/go"
export GOBIN="$HOME/.local/bin"
export CARGO_HOME="$HOME/.cargo"
export NVM_DIR="$HOME/.nvm"
export PYENV_ROOT="$HOME/.pyenv"
export BUN_INSTALL="$HOME/.bun"

export PATH="$HOME/.local/bin:$HOME/dev-setup/bin:$GOPATH/bin:$CARGO_HOME/bin:$BUN_INSTALL/bin:/usr/local/bin:$PATH"

type node &>/dev/null && export PATH="$(dirname $(which node)):$PATH"
[ -d "$PYENV_ROOT/bin" ] && export PATH="$PYENV_ROOT/bin:$PATH"

. "$HOME/.cargo/env" 2>/dev/null
