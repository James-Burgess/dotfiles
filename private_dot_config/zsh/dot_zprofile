# ── Login-shell only — runs after .zshenv and before .zshrc ─────────────────
export ZSH="$HOME/.oh-my-zsh"

[ -f "$HOME/google-cloud-sdk/path.zsh.inc" ] && . "$HOME/google-cloud-sdk/path.zsh.inc"

# Welcome banner disabled — uncomment to re-enable:
# if [ -n "${SSH_CONNECTION:-}" ] && [ -x "$HOME/.local/bin/welcome" ]; then
#   exec "$HOME/.local/bin/welcome"
# fi
